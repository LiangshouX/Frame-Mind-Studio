package io.framemind.core.config;

import org.springframework.context.annotation.Configuration;

/**
 * 数据库配置类。
 * Spring Boot 通过 application.yml 中的 spring.datasource.* 属性自动配置
 * DataSource、EntityManager 和 TransactionManager。
 */
@Configuration
public class DatabaseConfig {
    // Spring Boot 通过 application.yml 中的 spring.datasource.* 属性自动配置
    // DataSource、EntityManager 和 TransactionManager
}
