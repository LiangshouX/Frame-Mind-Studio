package io.framemind.core.config;

import org.springframework.context.annotation.Configuration;

/**
 * Redis 配置类（v1 占位）。
 * 后续版本将扩展缓存和发布/订阅功能。
 */
@Configuration
public class RedisConfig {
    // v1 版本的 Redis 配置占位符。
    // 后续添加缓存和发布/订阅功能时将进行扩展。
}
